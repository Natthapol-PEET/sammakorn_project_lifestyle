python -m venv env

env\Scripts\activate

pip install -r requirements.txt

uvicorn --port 8000 --host 127.0.0.1 main:app --reload
uvicorn --port 8000 --host 192.168.0.102 main:app --reload
uvicorn --port 8000 --host 0.0.0.0 main:app --reload
